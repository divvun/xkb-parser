file = { SOI ~ (item)* ~ EOI }
item = _{ def | ifdef }
def = { "#define" ~ ident ~ hex }
ifdef = { ("#ifdef" ~ ident) | ("#endif") }

WHITESPACE = _{ " " | "\t" | NEWLINE }
COMMENT = _{ "/*" ~ (!"*/" ~ ANY)* ~ "*/" }

ident = @{ (ASCII_ALPHANUMERIC | "_" )+ }
hex = @{ "0x" ~ ASCII_HEX_DIGIT+ }
